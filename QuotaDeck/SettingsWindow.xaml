<Window x:Class="QuotaDeck.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Usage Settings"
        Width="410"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#14171E"
        FontFamily="Segoe UI Variable Display, Malgun Gothic, Segoe UI">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E8EAED"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E8EAED"/>
            <Setter Property="Margin" Value="0,7,0,0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="14,5"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="24,18,24,20">
        <TextBlock Text="Display" FontSize="13" FontWeight="SemiBold"/>
        <CheckBox x:Name="RemainChk" Content="Show remaining instead of used"/>
        <CheckBox x:Name="HorizontalChk" Content="Horizontal layout (services side by side)"/>
        <CheckBox x:Name="TopmostChk" Content="Always on top"/>
        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="Theme" VerticalAlignment="Center" Width="112"/>
            <ComboBox x:Name="ThemeCombo" Width="230" MaxDropDownHeight="360">
                <ComboBox.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <Border Background="#202631" Padding="8,5" Margin="0,5,0,2">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="#9FAFC2"
                                               FontSize="10.5"
                                               FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ComboBox.GroupStyle>
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Refresh interval" VerticalAlignment="Center" Width="112"/>
            <ComboBox x:Name="RefreshCombo" Width="110"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Opacity" VerticalAlignment="Center" Width="112"/>
            <Slider x:Name="OpacitySlider" Width="150" Minimum="0.5" Maximum="1"
                    TickFrequency="0.05" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
            <TextBlock x:Name="OpacityLabel" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#9AA3AE"/>
        </StackPanel>

        <TextBlock Text="Warning thresholds" FontSize="13" FontWeight="SemiBold" Margin="0,18,0,0"/>
        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="Yellow (used ≥)" VerticalAlignment="Center" Width="112"/>
            <Slider x:Name="YellowSlider" Width="150" Minimum="30" Maximum="95"
                    TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
            <TextBlock x:Name="YellowLabel" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#9AA3AE"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="Red (used ≥)" VerticalAlignment="Center" Width="112"/>
            <Slider x:Name="RedSlider" Width="150" Minimum="50" Maximum="100"
                    TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
            <TextBlock x:Name="RedLabel" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#9AA3AE"/>
        </StackPanel>

        <TextBlock Text="System" FontSize="13" FontWeight="SemiBold" Margin="0,18,0,0"/>
        <CheckBox x:Name="AutostartChk" Content="Start with Windows"/>
        <CheckBox x:Name="TaskbarChk" Content="Show mini gauges in the taskbar"/>

        <TextBlock Text="Accounts" FontSize="13" FontWeight="SemiBold" Margin="0,18,0,0"/>
        <TextBlock x:Name="ClaudeStatusText" Foreground="#9AA3AE" Margin="0,6,0,0"/>
        <TextBlock x:Name="CodexStatusText" Foreground="#9AA3AE" Margin="0,3,0,0"/>
        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="ClaudeLoginBtn" Content="Claude login" Margin="0,0,8,0"/>
            <Button x:Name="CodexLoginBtn" Content="Codex login"/>
        </StackPanel>
        <TextBlock Text="Login opens in a new terminal window. Changes are picked up automatically."
                   FontSize="10.5" Foreground="#6B727C" Margin="0,7,0,0" TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="ApplyBtn" Content="Apply" Margin="0,0,8,0"/>
            <Button x:Name="CloseBtn" Content="Close"/>
        </StackPanel>
    </StackPanel>
</Window>
